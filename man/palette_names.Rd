% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/catalogue.R
\name{palette_names}
\alias{palette_names}
\title{Palette names}
\usage{
palette_names(
  collection = NULL,
  type = NULL,
  n = NULL,
  include_review = TRUE,
  dataviz_friendly = NULL
)
}
\arguments{
\item{collection}{Optional collection name(s) to keep: \code{"retro"},
\code{"minimalism"}, \code{"kawaii"} and/or \code{"avantgarde"}. \code{NULL} (default) keeps
all four.}

\item{type}{Optional matching type(s) to keep. Accepts either the printed
names (\code{"bicolor"}, \code{"tricolor"}, \code{"four-color"}, \code{"multicolor"}) or the
short codes used in palette IDs (\code{"bi"}, \code{"tri"}, \code{"four"}, \code{"multi"}).}

\item{n}{Optional palette size(s) to keep, as a number of colours.}

\item{include_review}{Whether to include palettes that contain a provisional
swatch reading. \code{TRUE} by default, so nothing is hidden; the \code{status}
column marks them and \code{\link[=palettes_needing_review]{palettes_needing_review()}} lists them.}

\item{dataviz_friendly}{Optionally keep only palettes that screen well for
data visualisation (\code{TRUE}), or only those that do not (\code{FALSE}). \code{NULL},
the default, keeps both. See the Data visualisation section.}
}
\value{
A character vector of palette IDs.
}
\description{
The stable IDs of the palettes matching a filter. Each ID is also the name
of an exported object, so \code{get_palette(palette_names("kawaii")[1])} and
\code{kawaii_bi_01} give the same colours.
}
\examples{
palette_names("avantgarde")
palette_names("minimalism", type = "bicolor")
palette_names(n = 4, dataviz_friendly = TRUE)
length(palette_names())
}
\seealso{
\code{\link[=palettes]{palettes()}} for the full catalogue.
}
